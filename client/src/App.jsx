import React, { PureComponent } from "react";
import Routing from "./Routing";

class App extends PureComponent {
    render(){
        return(
            <Routing />
        );
    }
}

export default App;
